# ============================================================
# Java / Maven / Spring Boot
# ============================================================
target/
*.class
*.jar
*.war
.mvn/
hs_err_pid*

# Allow vendored Clockify SDK artifacts (see repo/) — eliminates the
# GitHub Packages build-time PAT dependency.
!repo/**/*.jar

# IDE
.idea/
*.iml
.vscode/
.classpath
.project
.settings/
dependency-reduced-pom.xml

# OS / temp
.DS_Store
**/.DS_Store
*.log

# Agent tooling — local only, never part of the marketplace repo
.claude/
docs/superpowers/

# Secrets
.env
.env.*
!.env.example

# Session-state directory (transient agent memory)
.remember/
